import { Component, Input } from '@angular/core';

@Component({
  selector: 'tsl-category-cards',
  template: '',
  styleUrls: [],
})
export class CategoryCardsStubComponent {
  @Input() categoryId: string;
  @Input() objectTypeId: string;
}
