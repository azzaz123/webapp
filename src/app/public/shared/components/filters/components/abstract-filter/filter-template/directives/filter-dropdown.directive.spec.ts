import { FilterDropdownDirective } from './filter-dropdown.directive';

describe('FilterDropdownDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterDropdownDirective();
    expect(directive).toBeTruthy();
  });
});
