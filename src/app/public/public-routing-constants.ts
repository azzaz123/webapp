export enum PUBLIC_PATHS {
  LOGIN = 'login',
  REGISTER = 'register',
  WALL = 'wall',
  ITEM_DETAIL = 'item',
  SEARCH = 'search',
  USER_DETAIL = 'user',
  NOT_FOUND = '404',
}

export enum PUBLIC_PATH_PARAMS {
  ID = 'id',
  WEBSLUG = 'webSlug',
}
