import { environment } from '@environments/environment';

export const CATEGORIES_ENDPOINT = `${environment.baseUrl}api/v3/categories/keys`;
export const CATEGORIES_WITH_PRESENTATION_ENDPOINT = `${environment.baseUrl}api/v3/categories`;
