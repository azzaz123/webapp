import { ONGOING_TRANSACTION_TRACKING_STATUS } from '../ongoing-transaction-tracking-status.enum';

export const mapContextToOngoingTransactionTrackingStatus = () => {
  return ONGOING_TRANSACTION_TRACKING_STATUS.IN_TRANSIT;
};
