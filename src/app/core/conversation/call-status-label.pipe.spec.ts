import { CallStatusLabelPipe } from './call-status-label.pipe';

describe('CallStatusLabelPipe', () => {
  it('create an instance', () => {
    const pipe = new CallStatusLabelPipe();
    expect(pipe).toBeTruthy();
  });
});
