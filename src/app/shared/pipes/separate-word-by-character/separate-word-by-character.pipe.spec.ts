import { SeparateWordByCharacterPipe } from './separate-word-by-character.pipe';

describe('SeparateWordByCharacterPipe', () => {
  it('create an instance', () => {
    const pipe = new SeparateWordByCharacterPipe();
    expect(pipe).toBeTruthy();
  });
});
