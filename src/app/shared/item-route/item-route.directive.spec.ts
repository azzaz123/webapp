import { ItemRouteDirective } from './item-route.directive';

describe('ItemRouteDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemRouteDirective();
    expect(directive).toBeTruthy();
  });
});
