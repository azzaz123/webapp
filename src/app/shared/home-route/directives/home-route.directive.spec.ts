import { HomeRouteDirective } from './home-route.directive';

describe('HomeRouteDirective', () => {
  it('should create an instance', () => {
    const directive = new HomeRouteDirective();
    expect(directive).toBeTruthy();
  });
});
