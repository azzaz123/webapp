import { AdSlotGroupDirective } from './ad-slot-group.directive';

describe('AdSlotGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new AdSlotGroupDirective();
    expect(directive).toBeTruthy();
  });
});
