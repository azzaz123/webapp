import { AdSlotShoppingDirective } from './ad-slot-shopping.directive';

describe('AdSlotShoppingDirective', () => {
  it('should create an instance', () => {
    const directive = new AdSlotShoppingDirective();
    expect(directive).toBeTruthy();
  });
});
